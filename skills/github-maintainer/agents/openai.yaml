interface:
  display_name: "GitHub Maintainer"
  short_description: "Maintain clean GitHub repositories"
  default_prompt: "Inspect this repository with the maintainer profile, update documentation if needed, run hygiene checks, and prepare a clean commit."
