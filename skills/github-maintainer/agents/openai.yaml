interface:
  display_name: "GitHub Maintainer"
  short_description: "Maintain clean GitHub repositories"
  default_prompt: "Use $github-maintainer to inspect this repository, make the requested maintenance changes, run proportional hygiene checks, and prepare a clean handoff."
