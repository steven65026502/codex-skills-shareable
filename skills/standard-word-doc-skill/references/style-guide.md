# 标准 Word 样式规范

本文件是 `standard-word-doc` 的机器可读规范。`assets/standard-word-template.docx`
存在时，模板是标题、正文、列表、页眉页脚、页边距和编号体系的格式权威。表格
使用本文件定义的蓝色表头标准样式。只有模板缺失时，才使用本文的 fallback 样式创建
`.docx`。

## 核心原则

- 格式一致性优先于内容丰富度。
- 有模板时继承模板；无模板时使用本文件 fallback 样式。
- 不依赖 Word/WPS/LibreOffice/Pandoc 才能生成基础 `.docx`。
- 不在正文中手写章节编号，不用加粗正文模拟标题。
- 并列条目优先使用黑色菱形 `◆` 作为统一视觉符号；不要混用其它装饰 bullet。
- 不使用 Symbol、Wingdings、Wingdings 2、Wingdings 3 字体。
- 有模板时不得写入旧 fallback 蓝色标题或旧 fallback 字体颜色。
- 表格例外：无论生成还是修复，都必须使用本文定义的标准表格样式。
- 修复已有 `.docx` 时，默认从模板重建文档：源文件提供内容，模板提供格式。

## 当前模板基准

当前 `assets/standard-word-template.docx` 基于“浦东新区数据支撑子系统信创升级
可研方案”样式。脚本应优先使用模板中实际存在的样式：

| 用途 | 优先样式 |
| --- | --- |
| 封面/标题 | `Title` |
| 章节标题 | `Heading 1` 到 `Heading 7` |
| 正文 | `Body Ref`，缺失时用 `Normal` |
| 列表/条目 | `List Paragraph`，缺失时用 Word 列表样式 |
| 表格 | `Table Grid` |
| 页脚 | 模板自带页脚和页码字段 |

模板存在时，生成脚本不得主动调用 fallback 样式表覆盖标题和正文样式；修复脚本
不得保留源文档中的直接字体、颜色、字号等格式。表格必须额外应用下方“标准表格样式”。

## 标准表格样式

所有正式材料中的业务说明表、参数表、资源清单表和对比表都使用该样式：

| 项目 | 值 |
| --- | --- |
| 表头背景 | #1F5FAE |
| 表头字体 | #FFFFFF，加粗，居中，12pt |
| 正文字体 | #000000，常规字重，12pt |
| 正文奇数行背景 | #F3F6FB |
| 正文偶数行背景 | #FFFFFF |
| 边框颜色 | #C9C9C9 |
| 边框线宽 | 1pt |
| 垂直对齐 | 居中 |
| 表头水平对齐 | 居中 |
| 正文水平对齐 | 左对齐 |
| 表格宽度 | 撑满页面内容区 |

样式意图：表头醒目、正文具有轻微分区感，适合正式技术方案和可研材料中较长中文说明。

## Fallback 样式规范

以下规范仅在 `assets/standard-word-template.docx` 缺失时使用。

### 字体

| 项目 | 值 |
| --- | --- |
| 西文字体 | Times New Roman |
| 中文字体 | 仿宋 |
| 中文备选 | SimHei |
| 禁止字体 | Symbol, Wingdings, Wingdings 2, Wingdings 3 |

### 字号与段落

| 层级 | 字号 pt | 加粗 | 颜色 | 段前 pt | 段后 pt | 行距 |
| --- | ---: | --- | --- | ---: | ---: | --- |
| Title | 28 | 是 | #000000 | 0 | 4 | 1.0 |
| H1 | 22 | 是 | #000000 | 24 | 4 | 1.5 |
| H2 | 16 | 是 | #000000 | 8 | 4 | 1.5 |
| H3 | 15 | 是 | #000000 | 8 | 4 | 1.5 |
| H4 | 14 | 是 | #000000 | 4 | 2 | 1.5 |
| Body | 11 | 否 | #000000 | 0 | 8 | 1.15 |
| TableBody | 10 | 否 | #000000 | 2 | 2 | 1.0 |
| TableHeader | 12 | 是 | #FFFFFF | 4 | 4 | 1.15 |
| Footer | 9 | 否 | #808080 | 0 | 0 | 1.0 |

### 页面设置

| 项目 | 值 |
| --- | --- |
| 纸张 | A4 |
| 宽度 | 21cm |
| 高度 | 29.7cm |
| 上边距 | 2.54cm |
| 下边距 | 2.54cm |
| 左边距 | 3.17cm |
| 右边距 | 3.17cm |
| 装订线 | 0 |

### 列表规范

| 项目 | 规范 |
| --- | --- |
| parallel list | 使用 `◆内容` 形式，适合国产化适配项、能力清单、条件清单等并列说明 |
| bullet list | 除并列说明外，可使用 Word 内置 `List Bullet` 样式 |
| ordered list | Word 内置 `List Number` 样式 |
| 禁止 | 混用 `•`、`·`、`▪`、`—` 等装饰符号；`◆` 是唯一允许的手写并列项符号 |
| 多级列表缩进 | 每级 0.75cm |

### 表格规范

| 项目 | 值 |
| --- | --- |
| 表头背景 | #1F5FAE |
| 表头字体 | #FFFFFF，加粗，12pt |
| 奇数行背景 | #F3F6FB |
| 偶数行背景 | #FFFFFF |
| 边框颜色 | #C9C9C9 |
| 边框线宽 | 1pt |
| 表格宽度 | 撑满页面内容区 |
| 列宽 | 使用 DXA 固定值，避免百分比宽度 |

## API 接口详细设计结构

生成 API 详细设计文档时，每个接口优先按以下结构输出：

| 小节 | 内容 |
| --- | --- |
| 接口基本信息 | 接口名称、方法、路径、模块、鉴权、Content-Type、幂等性 |
| 请求参数 | 参数名、位置、类型、必填、说明、示例、约束 |
| 请求体字段 | 字段名、类型、必填、默认值、说明、校验规则 |
| 响应字段 | 字段名、类型、说明、示例 |
| 状态码和错误码 | HTTP 状态码、业务错误码、含义、处理建议 |
| 示例报文 | 请求 JSON、响应 JSON，使用代码块而不是普通正文 |
| 业务规则 | 权限、限流、重复提交、数据一致性、异常处理 |
| 测试要点 | 正常路径、异常参数、权限失败、重复提交、边界值 |

## 格式检查项

### 错误级别

| ID | 检查项 | 修复策略 |
| --- | --- | --- |
| E001 | 未批准的 Unicode bullet 字符作为段落开头 | 删除符号，应用 `List Bullet` 样式；`◆` 并列项除外 |
| E002 | 标题使用正文样式加粗和大字号模拟 | 按字号映射到 Heading 1-4 |
| E003 | 使用 Symbol/Wingdings 字体 | 替换为 Arial/微软雅黑 |
| E004 | 段落中存在硬换行 `<w:br>` 或 `\v` | 拆分为独立段落 |
| E005 | Heading 段落开头存在手写编号（如 `一、`、`第一章`、`1.1`、`1.1.1`） | 删除手写编号，保留 Heading 样式，由 Word/WPS 自动编号 |

### 警告级别

| ID | 检查项 | 修复策略 |
| --- | --- | --- |
| W001 | 正文字号出现两种以上不同值 | 有模板时重建文档；无模板时统一为 fallback 正文字号 |
| W002 | 表格宽度为百分比 | 转为 DXA 固定宽度 |
| W003 | 连续两个以上空段落 | 仅保留一个空段落 |
| W004 | 手动编号写在正文段落中 | 仅报告，需确认是否改为标题；Heading 段落中的手写编号自动删除 |
| W005 | 标题层级跳跃 | 仅报告，不自动修复 |

### 建议级别

| ID | 检查项 | 修复策略 |
| --- | --- | --- |
| S001 | 正文存在全角空格 | 仅报告，建议替换为半角 |
| S002 | 中英文之间没有空格 | 仅报告，避免误改专有名词 |
| S003 | 文档缺少页码字段 | 仅报告，建议添加或刷新页脚 |

## 审计报告要求

审计报告必须包含：

- 文件路径
- 问题总数
- 错误、警告、建议分级统计
- 每条问题的位置，优先使用段落索引、标题文字或表格索引
- 每条问题的规则 ID、说明和建议处理方式

## 修复输出要求

- 不覆盖原始文件。
- 修复后的 `.docx` 文件名追加 `_repaired`。
- 同时生成 `_repair_summary.md`，说明自动修复项和仍需人工确认项。
