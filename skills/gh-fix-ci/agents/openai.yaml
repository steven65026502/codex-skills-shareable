interface:
  display_name: "GitHub Fix CI"
  short_description: "Debug failing GitHub Actions CI"
  icon_small: "./assets/github-small.svg"
  icon_large: "./assets/github.png"
  default_prompt: "Use $gh-fix-ci to inspect failing GitHub Actions checks, summarize the root cause, and apply a focused fix when the request already authorizes changes."
